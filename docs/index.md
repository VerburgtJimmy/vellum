---
title: Vellum
description: Markdown documentation sites for Laravel.
---

Vellum turns a folder of Markdown into a documentation site: sidebar, search, light and dark theme, and static export.

Authoring is **markdown plus components**. Built-in callouts, tabs, steps, and cards stay as `:::` directives. `<x-…>` is how you add your own Blade components, plus allowlisted `env`, `config`, and `route` tags. There is no `{{ }}`, no Blade directives, and no `@php` in Markdown.

:::note
Pre-1.0: the API may change.
:::

:::cards
::card[Installation](/docs/getting-started/installation){icon=book}
::card[Components](/docs/components){icon=box}
::card[Extending](/docs/extending){icon=puzzle}
::card[Changelog](/docs/changelog){icon=list}
:::
